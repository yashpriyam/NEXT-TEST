/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com'], // Add 'images.unsplash.com' to allowed domains
      },
};

export default nextConfig;
